# GRPC client settings
grpc:
    host: calendar
    port: 55555

# HTTP server setting
http:
    host: calendar-ws
    port: 8080

# logging settings
logger:
    filepath: log.json
    level: debug # debug, info, warn, error
