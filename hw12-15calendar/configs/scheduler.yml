# queue settings
queue: 
    host: rabbitmq
    port: 5672
    user_name: guest
    password: guest
    name: event-queue

# database settings
db:
    host: postgres
    port: 5432
    user: event-db
    password: event-db
    name: event-db
